// This just helps identify the context of some string types in the code - as an ID string
export type ID = string

// ISO String for creation of objects
export type Time = {
    created: string,
    last_updated: string
}