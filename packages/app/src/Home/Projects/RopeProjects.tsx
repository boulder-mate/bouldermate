import { useNavigation } from "@react-navigation/native";
import { useEffect } from "react";
import { View, Text } from "react-native";

export const Ropes = (args) => {
  return (
    <View>
      <Text>Ropes!</Text>
    </View>
  );
};
