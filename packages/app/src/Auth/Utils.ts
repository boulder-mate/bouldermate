// Login function supporting username or email login
async function authorise(password: string, username?: string, email?: string) {
  // Query the backend
  // Receive auth token
}

// User creation function
async function createUser(password: string, username?: string, email?: string) {
  // Query the backend
  // Receive auth token
}
